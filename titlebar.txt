package com.Mahad.coffeeemahad

import android.annotation.SuppressLint
import androidx.compose.foundation.Image
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.size
import androidx.compose.material3.ExperimentalMaterial3Api
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.material3.TopAppBar
import androidx.compose.material3.TopAppBarDefaults
import androidx.compose.runtime.Composable
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.ui.Modifier
import androidx.compose.ui.res.painterResource
import androidx.compose.ui.text.style.TextAlign
import androidx.compose.ui.tooling.preview.Preview
import androidx.compose.ui.unit.dp
import com.Mahad.coffeeemahad.ui.theme.CoffeeeMahadTheme

@Preview
@Composable
fun App_Preview() {
    CoffeeeMahadTheme {}

    App(title = "Coffee Mahad")
}
//@Preview
@Composable
fun AppTitle(title: String) {

        Row( /*verticalAlignment = Alignment.CenterVertically,*/
            modifier = Modifier.fillMaxWidth(),
            horizontalArrangement = Arrangement.Center
            ) {
            Image(
                painter = painterResource(id = R.drawable.coffee_script),
                contentDescription = "Coffee Mahad Logo",
                modifier = Modifier.size(32.dp)
            )
            Text(text = title,
                textAlign = TextAlign.Center,
            //modifier = Modifier.weight(1f) //for logo left
            )
            //Spacer(modifier = Modifier.weight(1f)) //for title left
        }
}

@SuppressLint("UnusedMaterial3ScaffoldPaddingParameter")
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun App(title: String) {
    val selectedRoute = remember {
        mutableStateOf("menu")
    }
    Scaffold (
        topBar = {
            TopAppBar(
                colors = TopAppBarDefaults.smallTopAppBarColors(containerColor = MaterialTheme.colorScheme.primary),
                title = { AppTitle(title = title)}
            )
                 },
        content = {
            /*when(selectedRoute.value){
                Routes.offersPage.route-> OffersPage()
                Routes.MenuPage.route-> Text("Menu")
                Routes.InfoPage.route-> Text("Info")
                Routes.OrderPage.route-> Text("Order")
            }*/
        },
        //containerColor = MaterialTheme.colorScheme.primary,
        bottomBar = {
            NavBar(
            selectedRoute = selectedRoute.value,    //state hosting
            onChange = {
                    selectedRoute.value = it
            })
                    },

    )

}


